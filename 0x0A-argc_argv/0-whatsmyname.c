#include <stdio.h>

/**
 * main - prints the name of a programme
 * @argc: number of arguments passed to the function
 * @argv: argument vector of pointers to a strin
 * Return: always 0
 */
int main(int__argc attribute__((unused)), char *argv[])
{
 	printf("%s\n", argv[0]);
 	return (0);
}
